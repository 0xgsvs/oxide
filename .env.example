# Server
ADDR=0.0.0.0:3000

# PostgreSQL
DATABASE_URL=postgres://postgres:postgres@localhost:5433/oxide_dev?sslmode=disable

# Redis (rate limiting, caching, session store)
REDIS_URL=redis://127.0.0.1:6379

# JWT signing secret — generate with: openssl rand -base64 64
JWT_SECRET=change-me-to-a-random-64-char-string
